{
  "name": "Wildcat",
  "version": "0.0.1",
  "description": "A RSS feed app that compiles the full text versions of subscribed articles.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.13.3",
    "express": "^4.13.3",
    "mongoose": "^4.4.12",
    "unirest": "^0.5.0"
  }
}